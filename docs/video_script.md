"""
Demo Video Script – ReconMate Global Treasury Agent
Duration: 3–4 minutes

[0:00] INTRODUCTION
"Hi, I'm [Your Name], and this is ReconMate — our submission for the AI Marathon 2026
Global Treasury Agent challenge. SMEs deal with cross-border payments every day.
They receive RM42.50 in their bank, but the invoice was for USD10. Manually
reconciling this involves FX calculation, bank fee accounting, and careful logging.
ReconMate automates this entire workflow."

[0:30] PROBLEM STATEMENT
"Here's the pain point. An SME receives a payment proof from a client — maybe a
screenshot of a wire transfer, a third-party receipt, or a PDF invoice. They also have
their local bank statement. Both describe the same transaction, but in different formats,
different currencies, and with different fee structures. Matching them manually is
error-prone and expensive. ReconMate solves this with an agentic AI pipeline."

[1:00] TECH DEMO — UPLOAD & OCR
"Let me show you how it works. First, the user uploads a payment proof image.
Behind the scenes, PaddleOCR extracts the text — sender name, amount, currency,
reference number, and date. This is step 1 of the agent trace."

[1:20] TECH DEMO — MATCHING ENGINE
"Next, the matching engine compares the extracted proof against every row in the
bank statement. It checks references, amounts, dates, and applies FX conversion plus
bank fee tolerance. Matched transactions get a confidence score. Possible matches
and unmatched items are flagged for review. This is all deterministic — no LLM is
involved in the matching logic."

[1:50] TECH DEMO — AGENT DOCUMENTS
"Once the matching is complete, the agent generates two documents:
1. A Reconciliation Report for all successfully matched transactions.
2. A Discrepancy Summary for items that need human review.

These documents are generated by a Chutes-powered LLM, but here's the key:
the LLM explains structured facts. It does NOT calculate FX rates, bank fees,
or final match status. Those come from deterministic backend tools. This makes
the system finance-safe and auditable."

[2:30] FALLBACK DEMONSTRATION
"What if the LLM is unavailable or rate-limited? ReconMate gracefully falls back to
deterministic templates. The output structure stays identical, so the frontend
never breaks. Let me show you — I'll run the same request without the API key.
See? Same documents, same trace, same everything. The LLM fallback is invisible
to the user but critical for demo reliability."

[3:00] SUMMARY & FUTURE
"ReconMate is an agentic reconciliation system for SMEs. It combines OCR,
deterministic matching engines, and LLM-powered document generation to solve
cross-border payment reconciliation at scale. 

For the future, we'd integrate real-time FX APIs, support multi-bank format
imports, and add email/ Slack notifications for discrepancies. We're excited to
grow this into a full SaaS product for SMEs worldwide."

[3:20] CLOSING
"Thank you, judges. ReconMate — because every SME deserves enterprise-grade
reconciliation."

---
TECHNICAL NOTES FOR RECORDING
- Screen capture the FastAPI server running in one terminal
- Capture http://127.0.0.1:8000/docs (Swagger UI) showing endpoints
- Open http://127.0.0.1:8000/ in browser and show the Reconciliation Report
- Show the JSON payload from data/sample/reconciliation_payload.json
- Show the agent trace with paddle_ocr_extract, parse_payment_proof, match_transactions, chutes_generate_documents
- Show template fallback vs LLM mode side by side
"""
